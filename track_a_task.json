{
  "agent": "general",
  "prompt": "You are implementing Track A features for FlashPostgre PostgreSQL desktop app (Electron + React + TypeScript + Radix UI). Workspace: C:\\WorkSpace\\GitHub-Projects\\FlashPostgre.\n\nFEATURES TO IMPLEMENT (all in DataExplorer.tsx unless noted):\n\n1. SORT/FILTER CHIPS: Above the table in DataExplorer, add a Flex wrap row showing active sorts/filters as chips. Each chip shows: column name + direction arrow (↑ ↓) for sorts, or WHERE clause preview for filters. Each chip has an X button to remove it. When user clicks column header to sort: add chip. When filter input is non-empty: show WHERE chip. Below the table toolbar (row count / buttons row) and above the table, add this chip row. Styles in global.css: .fp-filter-chip (small badge, padding, background, border-radius, cursor pointer).\n\n2. COPY CELL/ROW/INSERT: In DataExplorer.tsx: (a) onDoubleClick on any td: copy that cell text to clipboard. (b) Add a small 'copy' icon button at the end of each row or as a hover action - simplest: add onClick to the row's first cell hover area. Actually: add a 'Copy row' button in the toolbar that copies the current page of rows as TSV. (c) Add 'Copy as INSERT' button in toolbar - generates INSERT INTO schema.table (cols) VALUES (vals) for current rows, copies to clipboard. Use navigator.clipboard.writeText().\n\n3. TYPE + NULLABLE INDICATORS: In the table header (thead > tr > th): (a) Colored dot before column name based on type: blue=#3b82f6 for numeric, gray=#6b7280 for text, yellow=#eab308 for date, purple=#a855f7 for json, green=#22c55e for bool. Use fields[] dataTypeID: 16/20/21/23/700/1700=numeric, 25/1043=text, 1082/1114/1184=date, 114/142/3802/3805=json, 16=bool. (b) Small gray '?' superscript if columns[i].is_nullable === 'YES'.\n\n4. CSV IMPORT DIALOG: Create src/components/CsvImportDialog.tsx - Radix Dialog with: (a) Drag-and-drop zone (dashed border, hover highlight). (b) File input button as fallback. (c) Parse CSV client-side: split by newline then comma (simple). (d) Preview first 10 rows in a table. (e) Column mapping: each CSV column → dropdown of DB columns. (f) 'Import' button: generate INSERT INTO schema.table (cols) VALUES (row1), (row2)... batch them in transaction via window.flashpostgre.query.transaction(). Add CSS: .fp-csv-dropzone (dashed border, min-height, text-align center, padding).\n\nAfter all changes: run 'npx tsc --noEmit' and 'npx vite build' in the workspace. Report pass/fail results.\n\nReport back to parent session mvs_18abafddc5f54c1d8648e8487869a613 when done."
}
