# Drop your app icon here as `icon.ico` (Windows, 256x256+) and `icon.png` (Linux, 512x512+)
# to replace the default Electron icon used by the installer.
#
# electron-builder will pick up these files automatically:
#   build/icon.ico   -> Windows .exe + NSIS installer
#   build/icon.png   -> Linux
#   build/icon.icns  -> macOS
